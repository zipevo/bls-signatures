PROJECT("get include dirs")
CMAKE_MINIMUM_REQUIRED(VERSION 3.14.0 FATAL_ERROR)

function(PRINT_VAR VARNAME)
    message(STATUS "${VARNAME}: ${${VARNAME}}")
endfunction()
PRINT_VAR("CMAKE_CXX_IMPLICIT_INCLUDE_DIRECTORIES")